# Workspace Rules for Style Heaven Mens Wear

- **Automatic Live Server Push**: Always stage (`git add`), commit with a descriptive message (`git commit`), and push (`git push origin main`) all code changes to GitHub `main` branch after completing a task so that live web servers (Render/Netlify) auto-deploy the updates immediately.
